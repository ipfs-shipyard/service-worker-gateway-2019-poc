# service-worker-gateway example

A IPFS gateway fully running on a Service Worker

## Usage

It is possible to:
- enable/disable the service worker
- fetch file content using /ipfs/<file-hash>

### Instalation and testing the example

* `$ npm install`
* `$ npm run build`
* `$ npm run start`